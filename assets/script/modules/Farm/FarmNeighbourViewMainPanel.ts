import * as fgui from "fairygui-cc";
import { BasePanel } from "modules/common/BasePanel";

export class FarmNeighbourViewMainPanel extends BasePanel {
    protected viewNode = {
        Name: <fgui.GLabel>null,
    };

    /* protected extendsCfg = [
        { ResName: "组件名", ExtendsClass: 拓展类 }
    ]; */

    InitPanelData() {
    }

    InitPanel() {
    }

    OpenPanel() {
    }

    ClosePanel() {
    }
}